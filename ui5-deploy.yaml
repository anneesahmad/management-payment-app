# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zmm.requisitionupload
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://vhfqmwd1wd01.sap.sarooj.com:44300
          client: '100'
        app:
          name: ZMMPRITEMUPL1
          description: S4HANA First Deployment
          package: ZFIORI_DEV
          transport: DP4K902534
        exclude:
          - /test/
